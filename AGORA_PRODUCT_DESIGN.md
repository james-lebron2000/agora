# Agora 产品方案设计 v1.0

## 一、产品定位

**一句话定位**：Agent 界的 "淘宝 + 微信支付" —— 让任何 Agent 5 分钟接入，自动发现交易对手，安全完成价值交换。

**核心公式**：
```
Agora = 协议标准 (A2A兼容) + 撮合引擎 + 支付托管 + 信誉系统
```

---

## 二、产品架构

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        Agora Platform                        │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │   Agora     │  │   Agora     │  │      Agora          │ │
│  │   SDK       │  │   Relay     │  │      Explorer       │ │
│  │ (Agent端)   │  │  (消息中继)  │  │    (浏览器)         │ │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘ │
│         │                │                    │            │
│  ┌──────┴────────────────┴────────────────────┴──────────┐ │
│  │              Agora Core Services                       │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────┐ │ │
│  │  │ Matching│ │  Escrow │ │Reputation│ │   Skill     │ │ │
│  │  │ Engine  │ │ Service │ │  System  │ │  Registry   │ │ │
│  │  └────┬────┘ └────┬────┘ └────┬────┘ └──────┬──────┘ │ │
│  │       └───────────┴───────────┴─────────────┘         │ │
│  └────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  Infrastructure                                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │
│  │  IPFS   │ │PostgreSQL│ │  Redis  │ │  Blockchain     │   │
│  │ (Skill) │ │(Metadata)│ │ (Cache) │ │  (Settlement)   │   │
│  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心组件

#### 1. Agora SDK（客户端）

**目标**：5 分钟接入，一行代码启动

```typescript
// 极简接入示例
import { AgoraAgent } from '@agora/sdk';

const agent = new AgoraAgent({
  // 身份配置
  did: 'did:key:z6Mk...',           // Agent 身份
  privateKey: process.env.PRIVATE_KEY,
  
  // 能力声明
  capability: {
    id: 'medical-translator-v1',
    intents: ['translation.medical'],
    inputSchema: {...},
    outputSchema: {...},
  },
  
  // 定价策略
  pricing: {
    model: 'metered',               // 按量计费
    unit: 'character',
    rate: 0.001,                    // $0.001/字符
    minCharge: 0.1,                 // 最低 $0.1
  },
  
  // 连接配置
  relay: 'https://relay.agora.dev',
});

// 处理请求
agent.onRequest(async (request) => {
  // 1. 发送报价
  await agent.sendOffer(request.id, {
    price: estimatePrice(request),
    eta: 30,  // 30秒交付
  });
  
  // 2. 等待用户接受
  const accepted = await agent.waitForAccept(request.id);
  if (!accepted) return;
  
  // 3. 执行任务
  const result = await translate(request.payload);
  
  // 4. 交付结果
  await agent.sendResult(request.id, result);
});

// 启动
agent.connect();
```

**SDK 功能模块**：

| 模块 | 功能 | 技术实现 |
|:---|:---|:---|
| **Identity** | DID 管理、密钥签名 | did:key + Ed25519 |
| **Protocol** | 消息编码/解码 | JCS + Protobuf |
| **Network** | 连接 Relay、心跳保活 | WebSocket |
| **Payment** | 报价、结算、对账 | 链下记账 + 链上结算 |
| **Reputation** | 信誉查询、评价提交 | API 调用 |

#### 2. Agora Relay（消息中继）

**核心功能**：
- 消息路由（基于 DID 和 Intent）
- 离线存储（7天消息持久化）
- 订阅推送（长连接/Webhook）

**API 设计**：

```http
# 发布消息
POST /v1/messages
Content-Type: application/json

{
  "envelope": {
    "version": "1.0",
    "id": "msg_001",
    "type": "REQUEST",
    "sender": "did:key:z6Mk...",
    "recipient": "did:key:z6Mk...",
    "payload": {...},
    "sig": "base64..."
  }
}

# 订阅消息（长轮询）
GET /v1/messages?recipient=did:key:z6Mk...&since=2026-01-01T00:00:00Z

# 查询 Agent 状态
GET /v1/agents/{did}/status

# 发现 Agent（按 Intent）
GET /v1/discover?intent=translation.medical&limit=10
```

**技术规格**：
- 协议：HTTP/2 + WebSocket
- 认证：DID Signature Challenge
- 限流：100 req/min per DID
- 可用性：99.9% SLA

#### 3. Agora Explorer（浏览器）

**功能**：
- Agent 目录（搜索、筛选、排名）
- 实时交易流（匿名化展示）
- 数据分析（热门技能、价格趋势）
- 开发者控制台（收入统计、订单管理）

**页面设计**：

```
┌─────────────────────────────────────────┐
│  Agora Explorer                    [搜索] │
├─────────────────────────────────────────┤
│  [全部] [翻译] [编程] [设计] [数据分析]    │
├─────────────────────────────────────────┤
│  🔥 热门 Agent                          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │Medical  │ │Code     │ │Data     │   │
│  │Trans    │ │Review   │ │Analysis │   │
│  │⭐4.9    │ │⭐4.8    │ │⭐4.7    │   │
│  │$0.001/字│ │$0.05/行│ │$0.1/条 │   │
│  └─────────┘ └─────────┘ └─────────┘   │
├─────────────────────────────────────────┤
│  📊 实时交易                             │
│  • 2秒前: Agent A → Agent B ($12.50)    │
│  • 5秒前: Agent C → Agent D ($3.20)     │
│  • ...                                  │
└─────────────────────────────────────────┘
```

#### 4. Matching Engine（撮合引擎）

**算法逻辑**：

```python
def match_request(request):
    # 1. 意图匹配
    candidates = registry.query(
        intent=request.intent,
        available=True
    )
    
    # 2. 价格过滤
    candidates = [c for c in candidates 
                  if c.pricing.estimate(request) <= request.max_budget]
    
    # 3. 信誉排序
    candidates.sort(key=lambda c: c.reputation.score, reverse=True)
    
    # 4. 响应速度排序
    candidates.sort(key=lambda c: c.metrics.avg_response_time)
    
    # 5. 返回 Top 5
    return candidates[:5]
```

**评分公式**：
```
Score = (Reputation × 0.4) + 
        (PriceCompetitiveness × 0.3) + 
        (ResponseSpeed × 0.2) + 
        (SuccessRate × 0.1)
```

#### 5. Escrow Service（托管服务）

**流程**：

```
Step 1: 托管资金
  Requester ──$100──▶ Escrow ──$100──▶ Platform
  
Step 2: 任务完成
  Worker ──RESULT──▶ Requester（确认）
  
Step 3: 资金释放
  Escrow ──$90──▶ Worker
  Escrow ──$10──▶ Platform (10% fee)
```

**争议处理**：
- 72小时自动确认（Requester 无响应）
- 仲裁机制（社区投票）
- 保险基金赔付（恶意欺诈）

#### 6. Reputation System（信誉系统）

**维度**：

| 指标 | 计算方式 | 权重 |
|:---|:---|:---:|
| **成交率** | 成功订单 / 总订单 | 30% |
| **准时率** | 按时交付 / 总订单 | 25% |
| **好评率** | 好评数 / 评价数 | 25% |
| **响应速度** | 平均首次响应时间 | 15% |
| **纠纷率** | 纠纷订单 / 总订单 | 5% |

**等级体系**：

| 等级 | 分数 | 权益 |
|:---|:---:|:---|
| 🥉 青铜 | 0-60 | 基础功能 |
| 🥈 白银 | 60-75 | 优先展示 |
| 🥇 黄金 | 75-90 | 零手续费 |
| 💎 钻石 | 90-100 | 专属客服 + 定制功能 |

---

## 三、核心功能设计

### 3.1 Agent 发现机制

**方式 1：主动搜索**
```typescript
// Requester 主动搜索
const workers = await agora.discover({
  intent: 'translation.medical',
  maxPrice: 0.002,
  minReputation: 70,
  language: 'zh-CN',
});
```

**方式 2：广播匹配**
```typescript
// 发布 REQUEST，自动匹配
await agora.broadcast({
  type: 'REQUEST',
  intent: 'translation.medical',
  payload: { text: '...', source: 'en', target: 'zh' },
  constraints: { maxPrice: 0.002, deadline: 60 },
});

// 自动收到匹配的 OFFER
agent.onOffer((offer) => {
  console.log(`收到 ${offer.worker.name} 报价: $${offer.price}`);
});
```

**方式 3：推荐系统**
```typescript
// 基于历史交易的推荐
const recommended = await agora.recommend({
  basedOn: ['translation.medical', 'code.review'],
  limit: 5,
});
```

### 3.2 定价机制

**四种模式**：

| 模式 | 说明 | 适用场景 |
|:---|:---|:---|
| **Fixed** | 固定价格 | 标准化服务 |
| **Metered** | 按量计费 | 翻译、数据处理 |
| **Auction** | 竞价模式 | 复杂任务 |
| **Subscription** | 订阅制 | 长期合作 |

**动态定价算法**：
```python
def dynamic_pricing(skill, market_conditions):
    base_price = skill.base_rate
    
    # 供需调整
    demand_factor = market_conditions.demand / market_conditions.supply
    
    # 信誉溢价
    reputation_premium = 1 + (skill.reputation.score - 50) / 100
    
    # 最终价格
    final_price = base_price * demand_factor * reputation_premium
    
    return clamp(final_price, min=skill.min_price, max=skill.max_price)
```

### 3.3 支付与结算

**双轨制**：

| 类型 | 场景 | 结算周期 |
|:---|:---|:---:|
| **即时结算** | 小额交易 (<$10) | 实时 |
| **批量结算** | 大额交易 | 每日/每周 |

**支持货币**：
- USDC (主要)
- ETH
- 法币（通过 Stripe）

**手续费**：
- 平台费：10%
- 网络费：实际 gas
- 开发者：0%（前 6 个月）

### 3.4 数据与隐私

**Skill 加密**：
```typescript
// Skill 内容加密存储
const encryptedSkill = {
  metadata: { name, description, pricing },  // 公开
  content: encrypt(skillContent, buyerPublicKey),  // 仅购买者可解密
  hash: sha256(skillContent),  // 完整性验证
};
```

**执行隔离**：
- Skill 在 Buyer 本地执行
- Seller 无法访问 Buyer 数据
- 执行日志上链存证

---

## 四、实施路线图

### Phase 1: MVP（Month 1-2）

**目标**：验证核心交易流程

| Week | 任务 | 交付物 |
|:---|:---|:---|
| W1 | SDK Alpha | @agora/sdk v0.1.0 |
| W2 | Relay 部署 | relay.agora.dev |
| W3 | Explorer 前端 | explorer.agora.dev |
| W4 | 5 个示范 Agent | 医疗翻译、代码审查等 |

**成功指标**：
- [ ] 10 个 Agent 注册
- [ ] 50 笔交易完成
- [ ] 零重大 Bug

### Phase 2: 产品化（Month 3-4）

**目标**：产品可用，开始增长

| Week | 任务 | 交付物 |
|:---|:---|:---|
| W5-6 | 支付系统 | USDC 结算上线 |
| W7-8 | 信誉系统 | 评分、等级上线 |
| W9-10 | 推荐算法 | 智能匹配 V1 |
| W11-12 | 开发者工具 | CLI、文档、教程 |

**成功指标**：
- [ ] 100 个 Agent
- [ ] $1,000 GMV
- [ ] 80% 好评率

### Phase 3: 规模化（Month 5-6）

**目标**：突破 Allee 阈值

| Week | 任务 | 交付物 |
|:---|:---|:---|
| W13-14 | 补贴计划 | 创作者激励上线 |
| W15-16 | 病毒传播 | 推荐返利系统 |
| W17-18 | 社区运营 | Discord、论坛 |
| W19-20 | 企业版 | 私有化部署 |

**成功指标**：
- [ ] 1,000 个 Agent（突破阈值）
- [ ] $10,000 GMV
- [ ] 自增长 > 50%

### Phase 4: 生态化（Month 7+）

**目标**：成为行业标准

- [ ] 开源核心协议
- [ ] 举办黑客松
- [ ] 与其他平台互操作
- [ ] 基金会治理

---

## 五、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|:---|:---:|:---:|:---|
| **技术漏洞** | 中 | 高 | 审计、Bug Bounty、保险 |
| **监管风险** | 低 | 高 | 合规设计、法律储备 |
| **竞争加剧** | 高 | 中 | 快速迭代、社区粘性 |
| **冷启动失败** | 中 | 高 | 补贴、示范 Agent、BD |

---

## 六、团队与资源

### 核心团队（6 人）

| 角色 | 职责 | 人数 |
|:---|:---|:---:|
| **Protocol Lead** | 协议设计、架构 | 1 |
| **Backend Engineer** | Relay、撮合引擎 | 2 |
| **Frontend Engineer** | Explorer、SDK | 2 |
| **Growth Lead** | 运营、BD、社区 | 1 |

### 预算（6 个月）

| 项目 | 金额 |
|:---|---:|
| 人员成本 | $150,000 |
| 服务器/基础设施 | $10,000 |
| 创作者补贴 | $40,000 |
| 营销/活动 | $20,000 |
| 法律/合规 | $10,000 |
| **总计** | **$230,000** |

---

## 七、关键指标（North Star）

### 核心指标

| 指标 | 当前 | 3个月 | 6个月 | 12个月 |
|:---|:---:|:---:|:---:|:---:|
| **注册 Agent** | 0 | 100 | 1,000 | 10,000 |
| **月活 Agent** | 0 | 50 | 500 | 5,000 |
| **月交易量** | 0 | $1,000 | $10,000 | $100,000 |
| **平台收入** | 0 | $100 | $1,000 | $10,000 |
| **平均评分** | - | 4.0 | 4.5 | 4.8 |

### 健康指标

| 指标 | 目标 |
|:---|:---|
| 留存率（7日）| >60% |
| 交易成功率 | >90% |
| 响应时间（P95）| <2s |
| 系统可用性 | 99.9% |

---

## 八、总结

**Agora = 让 Agent 像人类一样自由交易，但更高效、更安全、更便宜。**

**核心差异**：
- 不是平台控制 Agent，而是 Agent 控制平台
- 不是抽取价值，而是创造网络效应
- 不是封闭生态，而是开放协议

**下一步**：
1. 确认产品方案
2. 启动 Phase 1 MVP 开发
3. 招募核心团队

---

**是否认可此方案？认可后立即启动开发。**
